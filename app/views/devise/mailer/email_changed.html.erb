<p><%= t('mailer.email_changed.hello', email: @email) %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('mailer.email_changed.notify', email: @resource.unconfirmed_email) %>.</p>
<% else %>
  <p><%= t('mailer.email_changed.contacting', email: @resource.email) %>.</p>
<% end %>
