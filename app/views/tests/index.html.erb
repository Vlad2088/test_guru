<div class="container">
  <h1><%= t("tests.index.header") %></h1>

  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.count') %></th>
        <th colspan="3"><%= t('.actions') %></th>
      </tr>
    </thead>
      
    <%= render @tests %>
    
  </table>
</div>
