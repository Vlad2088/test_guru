<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %> 
  
  <div class="navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto"></ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <span class="navbar-text me-3">
            <%= t('.welcome') %>, <b><%= current_user.first_name %></b>
          </span>
          <% if admin? %>
            <li class="nav-item">
              <%= link_to t('.admin_panel'), admin_tests_path, class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to t('.tests'), tests_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.logout'), destroy_user_session_path, method: :delete, data: { confirm: t('.confirm') }, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('.login'), new_user_session_path, class: 'nav-link' %> 
          </li>
          <li class="nav-item">
            <%= link_to t('.registration'), new_user_registration_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
  </div>
</nav>
